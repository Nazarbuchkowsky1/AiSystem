# Copy to .env locally. On Railway set variables in the project dashboard.

PORT=3001
HOST=0.0.0.0
JWT_SECRET=change-me-long-random

TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_BOT_USERNAME=

AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_USERS=Users
AIRTABLE_FIELD_TELEGRAM_NAME=Telegram Username
AIRTABLE_FIELD_ROLE=Role

# Railway: attach a volume and set mount path, e.g. /data — keeps SQLite + uploads across deploys
# DATA_DIR=/data
